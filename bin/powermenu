#!/bin/env bash

choice=$(printf "Выйти\nПерезагрузить\nОтключить" | rofi -dmenu)
case "$choice" in
  Выйти) pkill -KILL -u "$USER" ;;
  Перезагрузка) systemctl reboot ;;
  Отключить) systemctl poweroff ;;
esac
